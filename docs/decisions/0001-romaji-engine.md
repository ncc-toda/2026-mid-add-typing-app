# ADR 0001: ローマ字判定は自前のオートマトンにする

- 日付: 2026-08-29
- 状態: 採用

## 状況

返信は日本語表示で、プレイヤーはローマ字でかなを追う。`si`/`shi`、撥音 `n`/`nn`、促音 `っ` など、一般的なゆらぎを正解にする必要がある。

## 判断

入力判定ライブラリは追加せず、ひらがなセグメントとローマ字候補のオートマトンをリポジトリ内で実装する。

## 理由

- MVP はローマ字のみで、かな配列や AZIK などは不要
- 出題の読みはこちらで固定できる
- 判定はテストで固定しやすく、依存を増やさない
